clear, close all;

%% load the sample data
[mesh,fileformat,attributes,solidID] = stlread('Test1.stl');


% first, generate a surface from the original image
% similar to demo_shortcuts_ex1.m

[img, v2smap]=s2v(memesh.Points,);
